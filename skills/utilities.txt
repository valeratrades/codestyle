# Per-skill sampling exponent for `cl review` (see pick.rs). 1.5..=3.0, unlisted skills get 1.5.
# Raising it spaces a skill's own runs more evenly, and wins it a larger share against lower-set ones.
codebase_primitives 2.5
