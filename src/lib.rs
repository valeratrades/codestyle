#![allow(unused_features)] // `default_field_values` is gated on older nightly but stabilized on newer; keep the gate for the former without the latter warning
#![feature(default_field_values)]
pub mod rust_checks;
